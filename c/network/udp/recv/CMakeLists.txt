cmake_minimum_required(VERSION 2.8.4)
project(recv C)

if(WIN32)
    set(source_files ${source_files} src/recv_windows.c)
else()
    set(source_files ${source_files} src/recv_unix.c)
    add_definitions(-Wall -std=c99)
endif()
add_executable(recv ${source_files})
