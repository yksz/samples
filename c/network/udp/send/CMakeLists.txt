cmake_minimum_required(VERSION 2.8.4)
project(send C)

if(WIN32)
    set(SOURCE_FILES ${SOURCE_FILES} src/send_windows.c)
else()
    set(SOURCE_FILES ${SOURCE_FILES} src/send_unix.c)
    add_definitions(-Wall -std=c99)
endif()
add_executable(send ${SOURCE_FILES})
