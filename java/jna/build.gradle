apply plugin: 'application'

repositories {
    mavenCentral()
}

dependencies {
    compile 'net.java.dev.jna:jna:3.5.2'
}

run {
    main = "JNAMain"
    classpath = sourceSets.main.runtimeClasspath
    systemProperties = ["jna.library.path": "lib"]
}
